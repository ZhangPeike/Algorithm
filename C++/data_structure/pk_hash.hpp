#include <cstdlib>
#include <iostream>

namespace pk_structure {
template <typename T> struct pk_hash {
  // m is the size of the container
  pk_hash(int m) {}
  // TODO: use shared_ptr
};
} // namespace pk_structure